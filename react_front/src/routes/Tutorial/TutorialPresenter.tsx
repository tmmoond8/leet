import React from 'react';
import styled from '../../styles/typed-components';

interface IProps {

}

const StyledLink = styled.a`

`;

const TutorialPresenter = (props: IProps) => {
  return (
    <StyledLink>기본 문제</StyledLink>
  )
}

export default TutorialPresenter;